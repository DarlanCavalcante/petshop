# ============================================
# CONFIGURAÇÕES DO PETSHOP SYSTEM
# ============================================
# IMPORTANTE: Copie este arquivo para .env e configure com valores reais!
# Windows: copy .env.example .env
# Linux/Mac: cp .env.example .env

# ============================================
# SEGURANÇA - OBRIGATÓRIO ALTERAR EM PRODUÇÃO
# ============================================

# Senha do banco de dados (mínimo 12 caracteres)
# Gere uma senha forte: https://passwordsgenerator.net/
MYSQL_ROOT_PASSWORD=MUDE_ISSO_senha_forte_aqui_123

# Chave secreta para JWT (mínimo 32 caracteres)
# Gerar no PowerShell: [Convert]::ToBase64String([byte[]](1..32|%{Get-Random -Max 256}))
# Gerar no Linux/Mac: openssl rand -hex 32
SECRET_KEY=MUDE_ISSO_gere_uma_chave_aleatoria_com_openssl

# ============================================
# Configurações antigas (manter para compatibilidade)
# ============================================
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=petshop
DB_USER=app_petshop
DB_PASS=AppPet$2025!

# ============================================
# ⚠️ NÃO SUBA O ARQUIVO .env PARA O GIT!
# O arquivo .gitignore já está configurado
# ============================================
